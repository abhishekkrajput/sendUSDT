import { providers, Contract, Wallet } from "ethers";
// run a simple script to check if increaseAllowance exists on USDT BSC
// actually we don't have private key, just check ABI or call it via staticCall
